config = {}

config.defaultPed = `a_m_m_acult_01` -- default ped name 

-- List of spawn points
config.spawnPoints = {
    vector3(2727.41, 1363.52, 24.52),
    vector3(700.0, 600.0, 130.0), -- Add more spawn points as needed
    -- Add more spawn points as needed
}

config.maxDistance = 30 -- max distance before it sends you back

config.spawnBackMessage = "Change your ped bro -_-"
